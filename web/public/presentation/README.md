# Presentation video (optional)

Put a recorded walkthrough here as `demo.mp4`, then on `/presentation` set the URL to:

```text
/presentation/demo.mp4
```

Or paste a YouTube / Vimeo link in the page (saved to localStorage), or set:

```bash
NEXT_PUBLIC_PRESENTATION_VIDEO_URL=https://youtu.be/xxxxx
```
